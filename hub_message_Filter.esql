CREATE FILTER MODULE hub_message_Filter
    CREATE FUNCTION Main() RETURNS BOOLEAN
    BEGIN
        -- Devuelve TRUE si hay un precio especificado en la solicitud
        -- Esto hará que el mensaje vaya al módulo FilterProductsByPrice
        RETURN EXISTS(Environment.RequestEntrada.price[]);
    END;
END MODULE;